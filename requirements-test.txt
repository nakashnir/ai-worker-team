# Test dependencies for ai-worker-team
# Install with: pip install -r requirements-test.txt

# Core dependencies (match workers/Dockerfile)
pydantic==2.8.2
redis==5.0.8
psycopg2-binary==2.9.9
anthropic==0.40.0

# Test framework
pytest==8.0.0
pytest-mock==3.12.0

# Optional: coverage reporting
pytest-cov==4.1.0
